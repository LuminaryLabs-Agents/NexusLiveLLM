# DSK Bubble Kit Builder Prompt Run 003

Build the next cumulative Kit Builder app focused on DSK bubble architecture.

The app should teach that domains are bounded bubbles with membranes of commands, APIs, validation, events, descriptors, ledgers, and repeat-safe state.

Carry forward the previous capabilities:

- bounded action menu
- JSON contract preview
- capability ledger panel
- cleanup/fate panel
- debug host via window.GameHost.getState()

Add a domain bubble editor with fields for boundary, owned state, commands, events, idempotency keys, invariants, should-not-own, and expansion candidates.

Make every output reviewable and preserve failure logs.
