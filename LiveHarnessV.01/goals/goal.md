# LiveHarnessV.01 Goal

Build NexusLiveLLM into a versioned, ledger-driven, multi-agent game and app factory.

The harness should use large models to choose structure, smaller bounded agents to fill typed slots, deterministic tools to validate outputs, a reconciler to merge final files, and append-only ledgers to record every decision.
