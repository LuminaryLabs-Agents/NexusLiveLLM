# Action Router v001

Select the next action from the available action menu. Use JSON only. Prefer CONTINUE for normal progress, RECONCILE after child outputs are ready, SHOW_ADVANCED for unresolved dependencies, and STOP when the run should end.
